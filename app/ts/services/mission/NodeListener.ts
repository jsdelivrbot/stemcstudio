import RoomListener from '../../modules/rooms/services/RoomListener';
import MwNode from '../../modules/synchronization/MwNode';

/**
 * Adapter that listens to the RoomAgent and sends syncronization messages to the node.
 */
export default class NodeListener implements RoomListener {
    constructor(node: MwNode) {
        // Do something soon.
    }
}
