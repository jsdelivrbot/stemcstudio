export interface ModalDialogOptions {

    /**
     * Every dialog should have a title giving the context of the modality.
     */
    title: string;

    /**
     * Every modal dialog should have a message proving context and direction for the user.
     */
    message: string;
}
