const NEWLINE = '\n';

export default function (options: {}, tabString: string): string {
    // const _ = tabString;
    const lines: string[] = [];
    return lines.join(NEWLINE).concat(NEWLINE);
}
