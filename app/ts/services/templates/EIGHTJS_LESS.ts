export default function(): string {
    const lines = "" +
        "body { margin: 0; }\n" +
        "canvas { width: 600px; height: 600px }\n" +
        "#stats { position: absolute; top: 0; left: 0; }\n";
    return lines;
}
