//
//
//
export const THEMES_MODULE = "com.stemcstudio.themes";
/**
 * The name of the dialog service that implements the ThemesDialog interface.
 */
export const THEMES_DIALOG = `${THEMES_MODULE}.dialog`;

export const THEME_MANAGER = `${THEMES_MODULE}.manager`;
