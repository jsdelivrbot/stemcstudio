import Book from '../../Book';
import vectors from './vectors/chapter';

const geometry: Book = {
    code: 'GEOMETRY',
    title: 'Geometry',
    parts: [
        vectors
    ]
}

export default geometry;