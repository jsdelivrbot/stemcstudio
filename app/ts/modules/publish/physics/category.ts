import Category from '../Category';
import mechanics from './mechanics/book';

const physics: Category = {
    code: 'PHYS',
    title: 'Physics',
    parts: [
        mechanics
    ]
}

export default physics;