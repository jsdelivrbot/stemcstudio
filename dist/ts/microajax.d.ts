/**
 * The microAjax (class).
 */
class microAjax {
  /**
   * Constructs a microAjax object that executes an HTTP request against a URL. 
   */
  constructor(url: string, callback: (response: string) => void) {
  }
}