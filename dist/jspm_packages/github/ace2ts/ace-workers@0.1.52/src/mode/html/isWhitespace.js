System.register([], function(exports_1) {
    function isWhitespace(ch) {
        return ch === " " || ch === "\n" || ch === "\t" || ch === "\r" || ch === "\f";
    }
    exports_1("default", isWhitespace);
    return {
        setters:[],
        execute: function() {
        }
    }
});
